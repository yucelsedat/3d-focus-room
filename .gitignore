# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# User Media
public/uploads/*
public/data/media.json

# SQLite database & journal files (untracked — local dev data; recreate via `npm run migrate`)
prisma/dev.db
prisma/*.db-journal
prisma/*.db-shm
prisma/*.db-wal

# Graphify generated outputs (regeneratable artifacts)
graphify-out/*
!graphify-out/GRAPH_REPORT.md

# Per-room roomchat knowledge graphs (regeneratable from room content)
room-graphs/

# Per-room roomsession project workspaces (her oda kendi web projesini burada geliştirir)
room-projects/

# Per-room bluprint reconstruction çıktıları (proje klasöründen reconstruct ile üretilir)
room-blueprints/

# Claude Code yerel ajan durumu (planlar, hafıza, yerel ayarlar)
.claude/

# recall skill checkpoint/araştırma durumu (yerel, yeniden üretilebilir)
.recall/
